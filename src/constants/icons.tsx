import MaterialComunityIcons from '@expo/vector-icons/MaterialCommunityIcons';

export const Icon = ({ name, color, size }: any) => {
  return <MaterialComunityIcons name={name} color={color} size={size} />;
};